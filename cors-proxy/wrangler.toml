name = "cors-proxy"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[vars]
# Comma-separated list of allowed target hosts (empty = allow all)
ALLOWED_HOSTS = "httpbin.org,api.openai.com,api.anthropic.com,generativelanguage.googleapis.com,openrouter.ai,api.mistral.ai,api.together.xyz,api.groq.com,api.cohere.com,api.deepseek.com"
